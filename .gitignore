# config.js contains API credentials and must never be committed.
# Copy config.example.js to config.js and fill in your credentials locally.
# In CI (GitHub Actions), config.js is generated from GitHub repository secrets.
config.js
